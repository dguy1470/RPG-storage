
#defines our stats
class Stats:
    def __init__(self,HP,Mana,AD,MD,Def,MR,MS,Reg,Mreg,CD,Crit,Gou,Pen,STA,LS,Ten,AS,EN):
        self.HP = float(HP)
        self.Mana = float(Mana)
        self.AD = float(AD)
        self.MD = float(MD)
        self.Def = float(Def)
        self.MR = float(MR)
        self.MS = float(MS)
        
        self.Reg = float(Reg)
        self.Mreg = float(Mreg)
        self.CD = float(CD)
        self.Crit = float(Crit)
        self.Gou = float(Gou)
        self.Pen = float(Pen)
        
        self.STA = float(STA)
        self.LS = float(LS)
        self.Ten = float(Ten)
        self.AS = float(AS)
        self.EN = float(EN)

